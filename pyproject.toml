[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "aegnn"
version = "0.0.0"
description = "Asynchronous Event-based Graph Neural Networks"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
packages = ["aegnn"]
